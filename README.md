# student--database--timetable
a single-table database, a Node.js/Express REST API that exposes GET, POST, PUT, DELETE, and a simple HTML/JavaScript page that uses the API to perform CRUD from the browser
